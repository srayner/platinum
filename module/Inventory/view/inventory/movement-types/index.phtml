<?php
    $title = 'Inventory Movement Types';
    $this->headTitle($title);
    $this->headScript()->appendFile('/js/jquery.dataTables.min.js', 'text/javascript');
    $this->headScript()->appendFile('/js/inventory/movement-types/index.js', 'text/javascript');
?>

<h3><?php echo $this->escapeHtml($title); ?></h3>
<p><a href="<?php echo $this->url('inventory/default', array(
    'controller' => 'movementtypes',
    'action'=>'add'
));?>">Add new inventory movement type</a></p>

<div class="widget">
    <div class="widget-header">
        <h4><?php echo $this->escapeHtml($title); ?></h4>
    </div>
    <div class="widget-content">
        
        <?php
	    if(!$this->messages == null){
            echo $this->partial('message.phtml', array(
                'type' => 'success',
                'message' => $this->messages[0]
            ));
	    }
	    ?>
        
        <table class="data">
            <thead>
                <tr>
                    <th>Code</th>
                    <th>Name</th>
                    <th class="no-sort actions">Actions</th>
                </tr>
            </thead>
            <tbody>
            <?php foreach($types as $type) : ?>
            <?php $actions = '<a class="icon edit" title="Edit" href="' . $this->url(
	                 'inventory/default', array('controller' => 'movementtypes', 'action'=>'edit', 'id' => $type->id)) . '"></a>' . 
	               '<a class="icon delete" title="Delete" href="' . $this->url(
	                 'inventory/default', array('controller' => 'movementtypes', 'action'=>'delete', 'id' => $type->id)) . '"></a>';?>
                <tr>
                    <td><?php echo $this->escapeHtml($type->code);?></td>
                    <td><?php echo $this->escapeHtml($type->name);?></td>
                    <td><?php echo $actions;?></td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>
<div class="hidden" id="total-records"><?php echo $iTotalRecords;?></div>