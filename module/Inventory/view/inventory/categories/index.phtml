<?php
    $title = 'Inventory Categories';
    $this->headTitle($title);
    $this->headScript()->appendFile('/js/jquery.dataTables.min.js', 'text/javascript');
    $this->headScript()->appendFile('/js/inventory/categories/index.js', 'text/javascript');
?>

<h3><?php echo $this->escapeHtml($title); ?></h3>
<p><a href="<?php echo $this->url('inventory/default', array(
    'controller' => 'categories',
    'action'=>'add'
));?>">Add new inventory category</a></p>

<div class="widget">
  <div class="widget-header">
    <h4><?php echo $this->escapeHtml($title); ?></h4>
  </div>
  <div class="widget-content">
  
      <?php
	    if(!$this->messages == null){
            echo $this->partial('message.phtml', array(
                'type' => 'success',
                'message' => $this->messages[0]
            ));
	    }
	  ?>
	    
    <table class="data">
        <thead>
            <tr>
                <th>Name</th>
                <th class="no-sort actions">Actions</th>
            </tr>
        </thead>
        <tbody>
    
            <?php foreach($categories as $category) : ?>
            <?php $actions = '<a class="icon edit" title="Edit" href="' . $this->url(
	                 'inventory/default', array('controller' => 'categories', 'action'=>'edit', 'id' => $category->id)) . '"></a>' . 
	               '<a class="icon delete" title="Delete" href="' . $this->url(
	                 'inventory/default', array('controller' => 'categories', 'action'=>'delete', 'id' => $category->id)) . '"></a>';?>
            <tr>
              <td><?php echo $this->escapeHtml($category->name);?></td>
              <td><?php echo $actions;?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
  </div>
</div>
<div class="hidden" id="total-records"><?php echo $iTotalRecords;?></div>