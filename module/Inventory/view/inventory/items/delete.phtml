<?php
$title = 'Delete inventory item';
$this->headTitle($title);
?>
<h3><?php echo $this->escapeHtml($title); ?></h3>
<div class="widget">

    <div class="widget-header">
	    <h4>Delete inventory item</h4>
	</div>
	
	<div class="widget-content">
	
        <p>Are you sure that you want to delete
        '<?php echo $this->escapeHtml($item['item_code']); ?>',   
        '<?php echo $this->escapeHtml($item['short_description']); ?>'?
        </p>
        <?php
        $form = $this->form;
        $form->setAttribute('action', $this->url('inventory/default', array('controller' => 'items', 'action' => 'delete', 'id'=>$this->id)));
        echo $this->RenderForm($form);
        $url = $this->url('inventory/default', array('controller' => 'items', 'action' => 'delete', 'id'=>$this->id)); ?>
    
    </div>

</div>
