<?php
    $title = 'Sales Representatives';
    $this->headTitle($title);
    $this->headScript()->appendFile('/js/jquery.dataTables.min.js', 'text/javascript');
    $this->headScript()->appendFile('/js/sales/representative/index.js', 'text/javascript');
?>

<h3><?php echo $this->escapeHtml($title); ?></h3>
<p><a href="<?php echo $this->url('sales/default', array(
    'controller' => 'representative',
    'action'=>'add'
));?>">Add new sales representative</a></p>

<div class="widget">
    <div class="widget-header">
        <h4><?php echo $this->escapeHtml($title); ?></h4>
    </div>
    <div class="widget-content">
  
        <?php
	    if(!$this->messages == null){
                echo $this->partial('message.phtml', array(
                    'type' => 'success',
                    'message' => $this->messages[0]
                ));
	    }
	?>
	    
        <table class="data">
            <thead>
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Description</th>
                    <th class="no-sort actions">Actions</th>
                </tr>
            </thead>
            <tbody>
            <?php foreach($representatives as $representative) : ?>
            <?php $actions = '<a class="icon edit" title="Edit" href="' . $this->url(
	        'sales/default', array('controller' => 'representative', 'action'=>'edit', 'id' => $representative->getId())) . '"></a>' . 
	        '<a class="icon delete" title="Delete" href="' . $this->url(
	        'sales/default', array('controller' => 'representative', 'action'=>'delete', 'id' => $representative->getId())) . '"></a>';?>
                <tr>
                    <td><?php echo $this->escapeHtml($branch->getFirstname());?></td>
                    <td><?php echo $this->escapeHtml($branch->getLastname());?></td>
                    <td><?php echo $this->escapeHtml($branch->getDescription());?></td>
                    <td><?php echo $actions;?></td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>

<div class="hidden" id="total-records"><?php echo $iTotalRecords;?></div>

